version: '3.3'
services:
  user_db:
    image: test_user_db
    networks:
      - bookstore
    environment:
      MYSQL_ROOT_PASSWORD: asdfgh
      MYSQL_TCP_PORT: 3307
    ports:
      - "3307:3307"

networks:
  bookstore:
    external: true
